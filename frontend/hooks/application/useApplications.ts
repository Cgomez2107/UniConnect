/**
 * hooks/application/useApplications.ts
 * 
 * Hook que orquesta la lógica de aplicaciones.
 * Maneja US-011 (apply to request) y US-012 (review applications).
 * 
 * TODO: Implement
 * - applyToRequest(requestId, message) - send application
 * - getApplicationsForRequest(requestId) - for authors
 * - reviewApplication(applicationId, decision) - author only
 * - Handle loading, error, success states
 * - Convert domain errors to UI messages
 * - Check for duplicate applications
 */
export function useApplications() {
  return {
    // TODO: Implement
  }
}
