import type { Conversation } from "../entities/Conversation"

/**
 * Interface for Conversation repository.
 * Defines contract for 1:1 conversation data access.
 */
export interface IConversationRepository {
  // TODO: Define methods
  // - getById(id: string): Promise<Conversation | null>
  // - getByUser(userId: string, page?: number): Promise<Conversation[]>
  // - findOrCreate(participantA: string, participantB: string): Promise<Conversation>
  // - create(conversation: Conversation): Promise<void>
  // - updateLastActivity(id: string): Promise<void>
}
