import type { Application } from "../entities/Application"

/**
 * Interface for Application repository.
 * Defines contract for study request applications data access.
 */
export interface IApplicationRepository {
  // TODO: Define methods
  // - getById(id: string): Promise<Application | null>
  // - getByRequest(requestId: string): Promise<Application[]>
  // - getByApplicant(applicantId: string): Promise<Application[]>
  // - create(application: Application): Promise<void>
  // - update(application: Application): Promise<void>
  // - delete(id: string): Promise<void>
  // - getReceivedByAuthor(authorId: string): Promise<Application[]>
}
