import type { StudyGroup } from "../entities/StudyGroup"

/**
 * Interface for StudyGroup repository.
 * Defines contract for study group data access.
 */
export interface IStudyGroupRepository {
  // TODO: Define methods
  // - getById(id: string): Promise<StudyGroup | null>
  // - getByUser(userId: string): Promise<StudyGroup[]>
  // - create(group: StudyGroup): Promise<void>
  // - update(group: StudyGroup): Promise<void>
  // - delete(id: string): Promise<void>
  // - addMember(groupId: string, userId: string): Promise<void>
  // - removeMember(groupId: string, userId: string): Promise<void>
}
