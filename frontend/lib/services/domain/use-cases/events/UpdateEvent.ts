/**
 * Use case: Update event (admin only).
 * 
 * TODO: Implement
 * - Validate user is admin
 * - Validate event exists
 * - Update event entity
 * - Save via repository
 */
export class UpdateEvent {
  // TODO: constructor(private repo: IEventRepository) {}
  // TODO: async execute(id: string, payload: UpdateEventPayload): Promise<Event>
}
