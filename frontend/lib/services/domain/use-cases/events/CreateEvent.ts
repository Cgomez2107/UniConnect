/**
 * Use case: Create event (admin only).
 * 
 * TODO: Implement
 * - Validate user is admin
 * - Create event entity with SCHEDULED status
 * - Save via repository
 */
export class CreateEvent {
  // TODO: constructor(private repo: IEventRepository) {}
  // TODO: async execute(payload: CreateEventPayload): Promise<Event>
}
