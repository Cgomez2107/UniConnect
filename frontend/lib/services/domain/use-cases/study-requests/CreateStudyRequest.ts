/**
 * Use case: Create a new study request.
 * 
 * TODO: Implement
 * - Validate user is authenticated
 * - Validate user is enrolled in subject
 * - Create study request entity
 * - Save via repository
 */
export class CreateStudyRequest {
  // TODO: constructor(private repo: IStudyRequestRepository) {}
  // TODO: async execute(payload: CreateStudyRequestPayload): Promise<void>
}
