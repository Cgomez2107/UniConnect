/**
 * Use case: Review application (accept/reject).
 * 
 * TODO: Implement
 * - Validate user is author of request (owner)
 * - Validate application exists
 * - Update application status
 * - Save via repository
 */
export class ReviewApplication {
  // TODO: constructor(private repo: IApplicationRepository) {}
  // TODO: async execute(applicationId: string, decision: 'accept' | 'reject'): Promise<void>
}
