/**
 * Use case: Send message (US-015).
 * 
 * TODO: Implement
 * - Validate user is authenticated
 * - Validate users have accepted relationship
 * - Find or create conversation
 * - Create message entity
 * - Save via repository
 */
export class SendMessage {
  // TODO: constructor(private messageRepo: IMessageRepository, private convRepo: IConversationRepository) {}
  // TODO: async execute(conversationId: string, content: string): Promise<Message>
}
